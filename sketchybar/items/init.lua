require("items.menus")

-- items.spaces (the workspaces widget) is WM-specific and now shipped by the
-- enabled WM aspect as the `wm` Lua module, required from init.lua.
require("items.left")
require("items.right")
require("items.secondary")
